package AppProgram.Assignment1.CE203AssignmentCode2223;

import javax.swing.*;

public class CommandWindow {

    private ContainerFrame conFrame;
    private TextPanel txtPanel;

    CommandWindow(){
        conFrame = cf;
        add()
    }

    private JButton createCommandWindow() {
        JButton button = new JButton("Display window");
        button.addActionListener(e -> {
            if (e.getSource() == button){
                JOptionPane.
            }
        });
    }
}
